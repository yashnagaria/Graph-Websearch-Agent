# Configuration Guide

## Important Files

- `config/config.yaml`: main configuration file.
- `models/`: model adapter definitions and settings.

## Configuration Steps

1. Open `config/config.yaml`.
2. Set search provider values and any required API tokens.
3. Save the file and restart the application.
